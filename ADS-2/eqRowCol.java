class equalRowColPairs {
    public int equalPairs(int[][] grid) {
        int n = grid.length;
        int ans = 0;
        Map<List<Integer>, Integer> map = new HashMap<>();

        for(int r=0;r<n;r++){
            List<Integer> row= new ArrayList<>();
            
        }
        return ans;
    }
}